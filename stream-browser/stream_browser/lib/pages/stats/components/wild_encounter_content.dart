import 'package:flutter/material.dart';

class WildEncounterContent extends StatelessWidget {
  const WildEncounterContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
